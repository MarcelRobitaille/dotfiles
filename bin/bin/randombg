#!/bin/bash

while true; do
  f=$(shuf -n1 -e /home/marcel/Pictures/landscapes/*)
  wal -t -i "$f"
  # feh --bg-scale "$f"
  source ~/.cache/wal/colors.sh
  bspc config focused_border_color "$color5"
  bspc config normal_border_color "$color0"
  sleep 300
done
