#!/bin/bash

f=${1:-$(shuf -n1 -e /home/marcel/Pictures/landscapes/*)}
# wal -qt -i "$f"
wpg -s "$f"
$(dirname $0)/background_after_change
# bspc config focused_border_color "$color5"
# bspc config normal_border_color "$color0"

