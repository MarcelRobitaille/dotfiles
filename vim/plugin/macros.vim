" Macro to switch common.js to es6 imports
let @i = "^cwimportf=dwcwfrom lds("

" Macro to switch es6 import to common.js require
let @r = "0ciwconstf'bciw=wysa')irequire"

" Macro to fix typos in line
let @s = "$[s1z="

" Convert class method to arrow function
let @a = "^f(i = f{i=> "

" Convert word under cursor to JavaScript import importing word from material-ui
let @m = "yiwiimport A from '@material-ui/core/'P"
