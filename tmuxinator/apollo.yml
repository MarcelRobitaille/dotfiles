# vim: modelines=0

name: apollo
root: ~/code/apollo
windows:
  - nvim: nvim .
  - serve:
      layout: main-vertical
      panes:
        - npm run nodemon -- --exec babel-node .
        - npm run gulp
  - zsh:
