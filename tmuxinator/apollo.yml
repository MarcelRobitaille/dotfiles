# vim: modelines=0

name: apollo
root: ~/code/apollo
windows:
  - vim: vim .
  - serve:
      layout: main-vertical
      panes:
        - npm run nodemon -- --exec babel-node .
        - npm run gulp
  - zsh:
