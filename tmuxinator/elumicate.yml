# vim: modelines=0

name: elumicate
root: ~/code/elumicate/dashboard
windows:
  - nvim: nvim .
  - serve:
    layout: even-horizontal
    panes:
      # - ./serve
      - gulp
  - ssh: ssh elumicate
  - zsh:
